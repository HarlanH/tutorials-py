[project]
name = "osai-track-01-hello"
version = "0.1.0"
description = "Track 1, Hello OpenSymbolicAI: the five-minute first win."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
]

[tool.uv]
package = false
