[project]
name = "osai-track-05-read-only"
version = "0.1.0"
description = "Track 5, read_only: the flag that signals whether a primitive modifies state."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
]

[tool.uv]
package = false
