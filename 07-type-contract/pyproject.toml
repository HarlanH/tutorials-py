[project]
name = "osai-track-07-type-contract"
version = "0.1.0"
description = "Track 7, type annotations are the contract: how parameter and return types reach the LLM."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
]

[tool.uv]
package = false
