[project]
name = "osai-track-08-read-the-plan"
version = "0.1.0"
description = "Track 8, read the generated plan: the LLM writes a plan, your primitives run it."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
]

[tool.uv]
package = false
