[project]
name = "osai-track-17-design-execute"
version = "0.1.0"
description = "Track 17, DesignExecute: use control flow in LLM-generated plans."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
]

[tool.uv]
package = false
