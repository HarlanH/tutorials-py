[project]
name = "osai-track-18-loop-guard"
version = "0.1.0"
description = "Track 18, max_loop_iterations: cap loop iterations in DesignExecute plans."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
]

[tool.uv]
package = false
