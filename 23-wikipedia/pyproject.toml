[project]
name = "osai-track-23-wikipedia"
version = "0.1.0"
description = "Track 24, wikipedia: fetch articles as variables, analyse without touching the LLM context."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
]

[tool.uv]
package = false
