[project]
name = "osai-track-25-nested-models"
version = "0.1.0"
description = "Track 25: nested Pydantic models and list[Model] as primitive types."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
]

[tool.uv]
package = false
