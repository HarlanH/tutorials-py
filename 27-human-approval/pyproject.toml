[project]
name = "osai-track-27-human-approval"
version = "0.1.0"
description = "Track 27: human-in-the-loop mutation approval via execute_stepwise."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
]

[tool.uv]
package = false
