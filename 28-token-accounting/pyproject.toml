[project]
name = "osai-track-28-token-accounting"
version = "0.1.0"
description = "Track 28: read TokenUsage from metrics, simple to complex calculator tasks."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
]

[tool.uv]
package = false
