[project]
name = "osai-track-30-no-progress"
version = "0.1.0"
description = "Track 30: max_iterations as a no-progress circuit breaker in GoalSeeking."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
]

[tool.uv]
package = false
