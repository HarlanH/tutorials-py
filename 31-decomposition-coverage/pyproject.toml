[project]
name = "osai-track-31-decomposition-coverage"
version = "0.1.0"
description = "Track 31, decomposition coverage: two question shapes, two worked examples."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
]

[tool.uv]
package = false
