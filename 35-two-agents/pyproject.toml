[project]
name = "osai-track-35-two-agents"
version = "0.1.0"
description = "Track 35: two specialist agents — one for abbreviations, one for arithmetic."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
]

[tool.uv]
package = false
