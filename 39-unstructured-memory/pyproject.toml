[project]
name = "osai-track-39-unstructured-memory"
version = "0.1.0"
description = "Track 39: unstructured memory -- session notes appended to a plain text file."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
]

[tool.uv]
package = false
