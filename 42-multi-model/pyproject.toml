[project]
name = "osai-track-42-multi-model"
version = "0.1.0"
description = "Track 42: two models in one agent — text model plans, VLM sees."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
]

[tool.uv]
package = false
