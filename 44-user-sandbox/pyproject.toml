[project]
name = "osai-track-44-user-sandbox"
version = "0.1.0"
description = "Track 44: per-user data isolation enforced at the primitive level."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
]

[tool.uv]
package = false
