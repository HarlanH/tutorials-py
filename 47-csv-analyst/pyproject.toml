[project]
name = "osai-track-47-csv-analyst"
version = "0.1.0"
description = "Track 47: agent answers questions about a CSV file using pandas."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opensymbolicai-core>=0.7",
    "pandas>=2.0",
]

[tool.uv]
package = false
